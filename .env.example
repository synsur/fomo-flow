# Names only. Values live in .env (gitignored).
TELEGRAM_BOT_TOKEN=
REFERRAL_LINK=
# Writes the candidate posts. Without it kontrol still offers a shortlist, but every option
# is a deterministic template rather than written copy (VOICE.md, profphet_voice.generate).
OPENROUTER_API_KEY=
# Optional: overrides the model. Default openai/gpt-5.6-luna.
PROFPHET_MODEL=
